import React from 'react';
import { ScrollView, Text } from 'react-native';

export default function FeedScreen() {
  return (
    <ScrollView>
      <Text>Feed wideo - Mobile</Text>
      <Text>Tu będą Twoje wideo (React Native Video / Expo AV)</Text>
    </ScrollView>
  );
}
